{
  "name": "@aonyxbuddy/browser-client",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "build:tsc": "tsc --build",
    "build" : "bun build ./src/app.ts --minify --splitting --outfile=./dist/app.js",
    "build:dev" : " bun build ./src/app.ts --watch --minify --splitting --outfile=./dist/app.js"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "bun-types": "latest"
  },
  "dependencies": {
    "gifuct-js": "^2.1.2"
  }
}