import { Types } from "../index.js";
export declare function FilterBannedWords(streamEvent: Types.StreamEvent, bannedWords: string[], replacement?: string, caseSensitive?: boolean): Types.StreamEvent;
