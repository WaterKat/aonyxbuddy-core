{
  "name": "@aonyxbuddy/stream-elements",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "devDependencies": {
    "bun-types": "^1.0.1",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build:prod" : "bun build ./src/index.ts --outfile=\"./dist/bundle.ts\"",
    "build:dev" : "bun build ./src/index.test.ts --outfile=\"./dist/test.js\"",
    "build" : "bun run build:prod && bun run build:dev"
  }
}
